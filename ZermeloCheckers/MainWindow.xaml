<Window x:Class="ZermeloCheckers.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ZermeloCheckers"
        xmlns:viewModels="clr-namespace:ZermeloCheckers.ViewModels"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800"
>
    <Window.DataContext>
        <viewModels:BoardViewModel/>
    </Window.DataContext>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="200"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="100"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <local:Board
            Grid.Column="0"
            Grid.Row="0"
            Grid.RowSpan="2"
            x:Name="gameBoard"
            Height="400"
            Width="400"
            Figures="{Binding Figures}"
        >
        </local:Board>
        <Label Grid.Column="1" Grid.Row="0" Content="{Binding ActivePlayer}" />
        <Button Grid.Column="1" Grid.Row="1" IsEnabled="{Binding IsUndoEnabled}"  Command="{Binding UndoMoveCommand}">Undo</Button>
    </Grid>
</Window>
